process {
    withName: 'TAR' {
    publishDir       = [
        mode: params.publish_dir_mode,
        path: { "${params.outdir}/${meta.study_id}.${meta.patient}.${meta.sample}/metrics/${meta.tool}/" },
        pattern: "*{txt,out/*tgz}",
        saveAs: { "${it}" }
    ]
    }
}