process {
    withName: 'SAMTOOLS_CONVERT' {
    publishDir       = [
        mode: params.publish_dir_mode,
        path: { "${params.outdir}/${meta.study_id}.${meta.patient}.${meta.sample}/alignment/${meta.tool}/" },
        pattern: "*cram*",
        saveAs: { "${it}" }
    ]
    }
}